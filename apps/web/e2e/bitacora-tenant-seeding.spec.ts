import { test } from "@playwright/test";

test(
  "Bitacora: top-bar seeds tenant, in-page filter overrides — manual verification via quickstart.md Scenario 5",
  async () => {
    test.skip(true, "TODO: manual verification — see quickstart.md Scenario 5");
  },
);
